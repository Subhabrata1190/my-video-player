export class Youtube{

    constructor(public title:string, public url: string, public status: string, public approved: number,
        public like: number, public dislike: number, public currentStatus: string, public exitPlayProgress: number){

    }

}